name: test pr

on:
  pull_request:

jobs:
  publish:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v1
      - name: unit-tests
        continue-on-error: true
        run: |
          whoami
          pwd
          id  
